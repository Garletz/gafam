FROM debian:bookworm-slim

ENV DEBIAN_FRONTEND=noninteractive
ENV DISPLAY=:99
ENV HOME=/home/browser

RUN apt-get update && apt-get install -y --no-install-recommends \
    firefox-esr \
    xvfb \
    openbox \
    pulseaudio \
    xdotool \
    ffmpeg \
    python3 \
    procps \
    fonts-dejavu-core \
    fonts-liberation \
    && rm -rf /var/lib/apt/lists/*

RUN useradd -m -s /bin/bash browser && \
    mkdir -p /home/browser/.mozilla/firefox/profile_main && \
    mkdir -p /home/browser/.mozilla/firefox/profile_agent && \
    chown -R browser:browser /home/browser

COPY firefox-profiles/ /home/browser/.mozilla/firefox/
RUN chown -R browser:browser /home/browser/.mozilla

COPY stream.py /stream.py
COPY entrypoint.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh /stream.py

EXPOSE 6080

USER browser
WORKDIR /home/browser

CMD ["/entrypoint.sh"]
