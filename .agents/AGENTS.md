# Deployment Rules

- **Cloudflare Frontend**: To deploy the web interface to Cloudflare, simply execute `npx wrangler deploy` from within the `frontend/` directory. Do not pass a project name explicitly, it is handled automatically by the configuration file.
